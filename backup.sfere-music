#! /bin/bash
set -e

. /usr/local/share/hbackup.sh

volume=/usb
setup

echo "--- Saving /music"
snapshot mp3 \
  nhbackup --repo $volume \
	  --index $today/music \
	  --root /snap/mp3 \
	  --one-file-system \
	  --verbose \
	  --backup
echo

echo "--- Done"

exit $status
